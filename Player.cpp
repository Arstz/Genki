#include "Player.h"

void Player::move(/*sf::Vector2f moveVector*/)
{
	/*
	(*shape)[0].position += moveVector;
	(*shape)[1].position += moveVector;
	(*shape)[2].position += moveVector; //akkuratn0 bistryi
	*/
}
